# Memex Continuity v1 traceability matrix

Statuses describe branch `feat/memex-continuity-v1` at Phase 5 closing HEAD `0854797` plus the Final Integration
(Prompt F1) corrections in the working tree: priority-lane partition claims (D-034), Capsule privacy cascade
(D-035), resident-revision gate trigger (D-036) and the cross-phase end-to-end fixture
`test/continuity-final-integration.test.ts` (capture → outbox → P0/P1 worker → exact extraction → Chronicle →
injection → sync → purge → replay with crash injection, duplicate delivery and the 13 mandatory zero counts).
"Latest gate" is the Final Integration gate verdict recorded in `final-integration-gate.md`.

| Invariant | RFC section | Current status | Related code | Related schema | Related tests | Target phase | Verification method | Latest gate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CAPTURE | §7, §19 | Phase 2 PASS, Phase 3 preserved | `src/continuity-core.ts`, `scripts/continuity-hook.js` | v4 journal streams/blocks, tail guard, hash-bound checkpoints, capture gaps | incremental/partial/growing rewrite/hash/crash/process-race + 200-turn matrix; F1: journal bytes == transcript bytes after crash seams, interrupts, duplicate and grown-prefix compaction | 2 | rolling append/hash/crash E2E | Final Integration PASS |
| ACCOUNTABILITY | §7.3, §19 | Phase 1 PASS | `src/continuity-store.ts`, `src/fact-extractor.ts`, pipeline status | targets/items/failed ranges/jobs | exact range, retry/dead, lease-expiry recovery; F1: unaccounted closed exchanges 0; transient crash leaves an audit `retry` range, no failed-visible/dead | 1 | exact failed-visible range and durable state inventory | Final Integration PASS |
| MONOTONIC INGESTION | §8.4, §19 | Phase 1 PASS | `ingestPrefixExchanges()`, guarded `insertExchange()`, canonical `ingestArchiveExchanges()` | generation/hash/line/closure guards | CP2-before-CP1 regression test; F1: CP1 after CP2 ignored with zero row change | 1 | older prefix causes zero delete/regression | Final Integration PASS |
| EXACT EXTRACTION | §8.1, §19 | Phase 1 PASS | immutable target/items and `commitExtractionPage()` | fixed fence, cursor ordinal, exact generation state | concurrent insert/update, legacy watermark holes; F1: every cursor-passed exchange was present in a model envelope (presented-set check), 15/15 | 1 | model-time insert leaves suffix; stale update commits zero | Final Integration PASS |
| NO SAMPLING LOSS | §8.2, §19 | Phase 1 PASS | contiguous window prefix + target page cursor | target item states/cursor | seeded 37-item randomized drain, fact-cap E2E; F1: 1-window pages over a 15-item target drain contiguously to cursor 15/15 after a model crash | 1 | ordinal coverage equals `1..N`; capped suffix pending | Final Integration PASS |
| OPEN TURN | §8.3, §19 | Phase 2 PASS | parser generation + transactional latest lifecycle fence | checkpoint closure and exchange generation | Stop/Interrupt/PreCompact/final process and adversarial tests; F1: PreCompact-fenced turn excluded from the extraction target until its Stop closes it | 1–2 | wrong closure 0; old prefix cannot regress later boundary | Final Integration PASS |
| AUTHORITY | §11.3, §19 | Phase 5 PASS: assistant repeat lane demoted to a labeled context-only hint below current truth; WATCH only from verified incident patterns; Capsule context-only in WORK NOW | extractor validator, `continuity-identity`, `inject-core` lanes and bundle order | fact authority; context-only lanes; `incident_signatures.pattern_state` | full suite + recall tests q/r/s; 5B gate case 4 (TTL re-emission still verified-only); harness `assistant_above_truth` 0, `watch_false_positives` 0; F1: Capsule/tail baton context-only through rehydration; WATCH only after 2 verified episodes | 1 regression, 2–5 preserve | labeled-lane regression; assistant never above current truth; single-episode/candidate/remediated never WATCH | Final Integration PASS |
| CAPSULE TYPING | §4.2, §14, §19 | Phase 2 PASS | `src/continuity-core.ts`, `src/continuity-worker.ts` | work capsules + checkpoint state | exact shape/bounds, source existence/authority, generation/lease CAS, failure fallback, authenticated Luna; F1: Capsule model crash retried, strict patch accepted, stale Capsule supplemented by tail baton | 2 | independent stale-worker/authority audit | Final Integration PASS |
| CURRENT VS HISTORY | §4.3–4.4, §15, §19 | Phase 4 PASS | `src/chronicle.ts`, `src/fact-management.ts` (`applyFactMeaningMutation`, RETIRED/RESTORED), `src/fact-extractor.ts` slot resolver, consolidator temporal judge | schema v5 extended `fact_revisions` (event kinds, `projection_applied`, `chronicle_seq`), `chronicle_tombstones` | `test/continuity-chronicle.test.ts` a–i, legacy migration; spine crash stages; 4B injected-failure atomicity (event rejected after projection UPDATE, later-step failure after event, ASSERTED insert rollback, lost claim after facts+events+incidents); F1: extractor ASSERTED→CHANGED via slot resolution, duplicate delivery merged, older evidence historical, rejected commit marker rolls facts+events back | 4 | projection/event atomicity + rollback under injected failure | Final Integration PASS |
| GROUNDED CAUSE | §4.4, §15, §19 | Phase 4 PASS | `verifyGroundedField`, grounded-source union in `recordChronicleEvent`, extractor `change_context` validator, consolidator classifier notes, sync-import structural guard | `grounded_cause`/`rationale`/`problem` vs `classifier_note`, `evidence_authority`, `effective_at_source=peer` | tests b/j/k/v, extractor validation; 4B grounded-source union, ungrounded peer row rejection, peer grounded/note preservation, labeled CLI/MCP output; F1: source-cited cause stored, unsupported rationale demoted to classifier note; ungrounded authoritative 0 | 4 | no write path stores an unverified cause as authoritative; every grounded field cites a source | Final Integration PASS |
| TEMPORAL ORDER | §4.4, §16, §19 | Phase 4 PASS | `judgeCompetingEvidence`, `currentEffectiveTime`, timeline ordering, incident out-of-order remediation | `effective_at`/`effective_at_source`/`recorded_at`/`chronicle_seq` | tests f/i, consolidator temporal cases; 4B out-of-order sync generations (recorded order CHANGED→ASSERTED, effective order ASSERTED→CHANGED), late older incident episode stays remediated, consolidator fallback labeled `recorded`; F1: older-effective evidence after a newer CHANGED becomes historical ASSERTED (projection_applied 0) | 4 | reversed completion/effective-order tests | Final Integration PASS |
| RESIDENCY | §12.2, §19 | Phase 5 PASS: resident identity `(fact_id, semantic_generation, lifecycle_generation)` per epoch; corrections derived from residency (D-028); Capsule generation and hint lines (WATCH/TRACE) resident per epoch; Hot Evidence watermark reset on epoch change (D-030) | `src/inject-core.ts`, `readResidentRevisionCorrections`/`advanceContextEpoch` in `src/continuity-core.ts` | epoch resident/carry tuples, `capsule_generation_seen`, `last_retrieval_at`, `watch_emitted_json` | recall tests j/k/l; 5B gate cases 1–6; harness `duplicate_injections` 0, `repeated_sibling_evidence` 0; F1: resident revision staleness is itself an invalidation token (D-036) | 2–3 foundation, 5 complete | same revision suppressed; old-epoch residency never reused; rehydrated Capsule/evidence not repeated by the first prompt | Final Integration PASS |
| REVISION-AWARE INJECTION | §12.4/12.6, §19 | Phase 5 PASS: cheap gate before every expensive call; same fact new generation → `[MEMEX CORRECTION]` with earlier statement; inactive → `No longer active`; never-resident facts only via relevance; bounded correction drain before `memory_revision_seen` advances | `src/recall-gate.ts`, `src/memory-bundle.ts`, `src/inject-core.ts` | tuple residency + `session_memory_state` gate state (schema v6) | recall tests a–x, 5B gate cases 1–8, `recall-calibration.json` (stale 0, corrections 3/3, mandatory misses 0/20, hard cap 432/1,000); F1: workstream fact changed by a sibling is corrected on the next acknowledgement with 0 embeddings (D-036) | 5 complete | generation delta/correction tests, hard budget, stale-revision recall not swallowed | Final Integration PASS |
| SCOPE | §10, §19 | Phase 3 PASS; Phase 4B extended to Chronicle timelines; Phase 5B: candidate search workstream-scoped, Hot Evidence workstream-scoped and sibling-only | `src/continuity-identity.ts`, DB/fact/injection/MCP/sync paths, `readChronicleTimeline` visibility, `readHotEvidence` filters | projects/workspaces/workstream sessions + stable IDs on evidence/facts/recall/checkpoints | worktree/rename/remote/link/split/migration/binding/all MCP surfaces/sync/privacy; 4B timeline scope isolation; recall test m, 5B gate cases 3 and 6, harness `wrong_workstream_injections` 0; F1: cross-project injection 0, wrong-workstream injection 0, MCP trace outside scope is an error | 3 | ambiguous merge/leakage zero across stable scope matrix | Final Integration PASS |
| BRANCH TRUTH | §10.5, §19 | Phase 3 PASS; Phase 4B history labeled/hidden by scope; Phase 5B two-session feature/main divergence: main session never receives the workstream fact, feature session corrected on its own residency | low-level promotion guard + workstream-scoped extraction/Capsule; `projectTruthOnly` timeline, unmerged event label; residency corrections | `subject_key`, `promotion_state`, workspace/workstream IDs | Redis experiment vs main current, evidence/membership rejection, subject collision; 4B unmerged history hidden under project scope; 5B gate case 6, harness `same-project-different-workstreams` 0 leaks; F1: extracted facts stay `workstream` until explicit merged promotion; sibling workstream never sees them before promotion | 3 | unmerged workstream excluded from project current and project history | Final Integration PASS |
| OUTBOX | §9, §19 | Phase 2 PASS | immediate-writer serialized journal/checkpoint/capture+Capsule jobs | checkpoints + memory_jobs + Capsule checkpoint state | fsync/transaction/process-race seams, duplicate events, atomic Capsule lease completion; F1: priority-lane partition claim (D-034), crash at fsync/checkpoint/job seams with re-delivery convergence | 1–2 | no checkpoint/job/Capsule half-state | Final Integration PASS |
| RECOVERY | §9, §20, §19 | Phase 2 PASS | orphan-tail repair, short-journal epoch recapture, expired lease wake, hash retry/dead convergence, tail baton | pending/retry/running/dead/gap states | crash seams, short/hash corruption, retry exhaustion, worker failure; F1: pending fact_extract no longer blocks capture_index (D-034); stale-lease capture completion rejected; index/Capsule/extraction model crashes retried | 1–2 | restart state inventory and compact immediate fallback | Final Integration PASS |
| HOOK BOUNDARY | §6, §19 | Phase 5 PASS: skip path makes 0 embedding/vector/graph/model calls (measured through the embedding module's own counters); acknowledgements with pending state are vector-free; cold hook import ~140 ms without model load | unified continuity hook; pinned runtime; `recall-gate.ts`; `inject-core.ts` `needsVector` | event matcher/timeout registration | Phase 2 fixtures + recall tests a/t/w; 5B gate cases 1, 7, 8; harness ack embedding requests 85 → 0; F1: acknowledgement corrections and Capsule carry with 0 embeddings | 2, 5 | no-op prompt embedding 0 | Final Integration PASS |
| POSTCOMPACT INDEPENDENCE | §6.2, §19 | Phase 2 PASS | PreCompact + SessionStart(compact); PostCompact telemetry only | context epoch/latest checkpoint | 200-turn zero-PostCompact fixture; F1: three compactions (auto, manual same-turn double, auto) with zero PostCompact; rehydration miss 0 | 2 | zero-PostCompact end-to-end fixture | Final Integration PASS |
| MCP ACCESS | §13, §19 | Phase 4 PASS; Phase 5B: automatic injection never replaces the deep path — TRACE points at `trace_fact`, gate skips leave `trace_fact`/`search_facts` untouched, bounded timeline pagination re-verified per workload | `trace_fact` (query/fact_id/subject_key, timeline cursor, incidents, lanes, scope-mirrored visibility), `readChronicleTimeline`, CLI `facts history|explain` | current facts/events/incidents/exchanges/Hot Evidence | tests q/r/s; 4B isolation; recall test v; harness `mcp_trace_success`/`mcp_timeline_bounded` true in all 11 workloads; F1: trace_fact on origin and on the sync peer (purged source labeled unavailable) | 3 foundation, 4 complete, 5 preserved | explicit scope + bounded history pagination; sibling workstream leakage 0 | Final Integration PASS |
| PRIVACY | §20, §19 | Phase 4 PASS | terminal session guard, `purgeChronicleForSources`, event tombstone sync, incident recount | continuity cascade + `chronicle_tombstones` + incident recount | queued purge, Phase 4 test l/u, sync tombstone import; 4B origin purge → peer tombstones → stale third-device replay resurrects nothing; F1: Capsule derived from purged session removed (D-035); full residue table 0 across journal/checkpoint/job/exchange/vector/fact/event/incident/capsule; purge crash rollback; hook/worker/extraction/sync replay resurrects nothing | every phase | pending worker no resurrection; purged events tombstoned across devices | Final Integration PASS |
| NO SILENT LOSS | §19–20 | Phase 1 PASS; Phase 4B preserved (sync conflict and ungrounded rows are visible, incident duplicates are explicit no-ops) | recursive split, singleton failed-visible, exact pending query, sync `malformedRows` | exact range/fingerprint/error + dead target/job | deterministic failure, randomized coverage, legacy markers; 4B same-id conflict and schema-invalid generation reporting; F1: model crash → retry state visible, silent skipped pages 0 | 1 | failed range never completed; conflicts never silent | Final Integration PASS |
