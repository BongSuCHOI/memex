// 언어 이름은 **번역하지 않는다** — 잘못된 언어로 들어온 사용자가 자기 언어를 찾을 수
// 있어야 하므로 항상 endonym(그 언어로 쓴 이름)을 그대로 보여준다.
//
// 이 값이 en.mjs에 들어가면 "en 사전에 한글 0건" 규율이 깨지고, ko.mjs에만 두면 en 화면에서
// 사라진다. 그래서 **언어 무관 상수 모듈**로 분리하고, 소스 한글 린트의 경로 허용 목록과
// DOM의 `data-endonym` 마커 두 곳에서 구조적으로 면제한다(설계 §7.1).
export const ENDONYMS = Object.freeze({ en: 'English', ko: '한국어' });
