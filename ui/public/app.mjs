import {request,setToken} from './api.mjs';
import {esc,icon,btn,linkBtn,banner,errorCard,skeleton,empty,options,basename,short,number,copy} from './ui.mjs';
import {renderDetail,commandModal} from './details.mjs';
import * as overview from './pages/overview.mjs';import * as facts from './pages/facts.mjs';import * as conversations from './pages/conversations.mjs';import * as taxonomy from './pages/taxonomy.mjs';import * as graph from './pages/graph.mjs';import * as activity from './pages/activity.mjs';import * as settings from './pages/settings.mjs';
const routes={'/':overview,'/facts':facts,'/conversations':conversations,'/taxonomy':taxonomy,'/graph':graph,'/activity':activity,'/settings':settings};
const navigation=[['/','grid','개요'],['/conversations','chat','대화 원장'],['/facts','memory','기억·사실'],['/taxonomy','folder','분류'],['/graph','graph','지식 지도'],['/activity','activity','활동 · 추적'],['/settings','settings','관리']];
// 조회 기본값은 전체 프로젝트(조회 전용)다. 실제 주입 범위는 "현재 프로젝트 + 공통 기억"이며 상단에 상시 표시한다.
export const SCOPE_HINT='주입은 작업 중인 프로젝트 + 공통 기억 기준입니다. 전체 프로젝트는 조회 전용 범위입니다.';
export const DEFAULT_SCOPE='all';
const scopeKeys=['scope','project','includeGlobal','workspace','workstream'];const panelKeys=['panel','item','panelTab'];const main=document.querySelector('#main'),detail=document.querySelector('#detail'),modal=document.querySelector('#modal');
let bootstrap,connected=false,eventSource,pageCleanup,drawerCleanup,pageAbort,drawerAbort,lastPageKey='',lastDrawerKey='',pageGeneration=0,drawerGeneration=0,toastTimer,loading=false,unread=false;const payloads=new Map();
let prefs={theme:'light',density:'comfortable',korean:true,live:false};try{Object.assign(prefs,JSON.parse(localStorage.getItem('memex.workspace.preferences')||'{}'));}catch{}
const media=matchMedia('(prefers-color-scheme: dark)');function applyPrefs(){document.documentElement.dataset.theme=prefs.theme==='system'?(media.matches?'dark':'light'):prefs.theme;document.documentElement.dataset.density=prefs.density;}applyPrefs();media.addEventListener('change',applyPrefs);
function savePrefs(values){prefs={...prefs,...values};try{localStorage.setItem('memex.workspace.preferences',JSON.stringify(prefs));}catch{}applyPrefs();renderShell();}
function currentScope(){const p=new URL(location.href).searchParams;const type=p.get('scope')||(p.get('project')?'project':DEFAULT_SCOPE);const s={scope:type};if(type==='project'){s.project=p.get('project')||'';s.includeGlobal=p.get('includeGlobal')==='0'?'0':'1';if(p.get('workspace'))s.workspace=p.get('workspace');if(p.get('workstream'))s.workstream=p.get('workstream');}return s;}
function href(path,query={}){const u=new URL(path,location.origin);for(const [k,v]of Object.entries(currentScope()))u.searchParams.set(k,v);for(const [k,v]of Object.entries(query)){if(v!==null&&v!==undefined&&v!=='')u.searchParams.set(k,String(v));else u.searchParams.delete(k);}return u.pathname+u.search;}
function routePath(){let p=location.pathname;if(p==='/dashboard')return '/';if(p==='/pipeline')return '/settings';const match=p.match(/^\/(facts|conversations)\/([^/]+)$/);if(match)return '/'+match[1];return p;}
function canonicalize(){const u=new URL(location.href);if(u.pathname==='/dashboard')u.pathname='/';if(u.pathname==='/pipeline'){u.pathname='/settings';u.searchParams.set('tab','diagnostics');}const match=u.pathname.match(/^\/(facts|conversations)\/([^/]+)$/);if(match){u.pathname='/'+match[1];if(match[1]==='facts'){u.searchParams.set('panel','fact');u.searchParams.set('item',decodeURIComponent(match[2]));}else u.searchParams.set('session',decodeURIComponent(match[2]));}
 // /facts?fact=<id>는 /facts/<id>와 같은 상세 패널을 연다(#26). 다른 화면의 fact= 필터는 건드리지 않는다.
 if(u.pathname==='/facts'&&u.searchParams.get('fact')){u.searchParams.set('panel','fact');u.searchParams.set('item',u.searchParams.get('fact'));u.searchParams.delete('fact');}
 if(!u.searchParams.has('scope')&&!u.searchParams.has('project')){try{const saved=JSON.parse(localStorage.getItem('memex.workspace.scope')||'null');if(saved)for(const[k,v]of Object.entries(saved))if(scopeKeys.includes(k))u.searchParams.set(k,v);}catch{}}
 // 저장된 범위도 없으면 기본값을 주소에 명시해 둔다 — 화면 범위는 항상 URL에서 읽을 수 있어야 한다.
 if(!u.searchParams.has('scope')&&!u.searchParams.has('project'))u.searchParams.set('scope',DEFAULT_SCOPE);
 history.replaceState(null,'',u.pathname+u.search);}
function go(url,replace=false){const u=new URL(url,location.origin);if(u.origin!==location.origin)return;const oldPage=pageKey();history[replace?'replaceState':'pushState'](null,'',u.pathname+u.search);document.querySelector('#sidebar').classList.remove('mobile-open');if(oldPage!==pageKey())window.scrollTo({top:0});render();}
function update(changes,replace=false){const u=new URL(location.href);for(const[k,v]of Object.entries(changes)){if(v===null||v===undefined||v==='')u.searchParams.delete(k);else u.searchParams.set(k,String(v));}go(u.href,replace);}
function pageKey(){const u=new URL(location.href);for(const k of panelKeys)u.searchParams.delete(k);u.searchParams.sort();return routePath()+u.search;}
function toast(message){const el=document.querySelector('#toast');el.textContent=message;el.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove('show'),4200);}
function open(kind,id,payload){if(payload)payloads.set(`${kind}:${id}`,payload);update({panel:kind,item:id,panelTab:null});}
function closeDetail(){update({panel:null,item:null,panelTab:null},true);}
function context(signal){const scope=currentScope();return {p:new URL(location.href).searchParams,scope,prefs,bootstrap,href,update,open,closeDetail,toast,savePrefs,setScope:changeScope,modal:showModal,confirm:(title,body,fn)=>showModal(title,banner(esc(body)),'확인',fn),invalidate(){lastPageKey='';render();},refreshDetail(){lastDrawerKey='';renderDrawer(true);},api(path,query={},options={}){return request(path,{...query,...scope},{signal,...options});}};}
function renderShell(){if(!bootstrap)return;const path=routePath(),scope=currentScope();const all=scope.scope==='all',global=scope.scope==='global';const project=scope.project||'';const selected=global?'global':all?'all':'project:'+project;const title=all?'전체 프로젝트':global?'공통 기억':basename(project);const env=bootstrap.environment;
 document.querySelector('#sidebar').innerHTML=`<a class="brand" href="${esc(href('/'))}" data-nav aria-label="Memex 개요"><span class="brand-symbol" aria-hidden="true"><i></i><i></i><i></i><i></i></span><span class="brand-name">memex<small>WORKSPACE</small></span></a><div class="nav-caption">MEMORY SPACE</div><nav aria-label="주 메뉴">${navigation.slice(0,5).map(([url,ico,t])=>`<a class="nav-item ${path===url?'active':''}" ${path===url?'aria-current="page"':''} href="${esc(href(url))}" title="${esc(t)}" data-nav>${icon(ico)}<span class="nav-label">${esc(t)}</span></a>`).join('')}<div class="nav-separator"></div><div class="nav-caption">OBSERVE & CONTROL</div>${navigation.slice(5).map(([url,ico,t])=>`<a class="nav-item ${path===url?'active':''}" ${path===url?'aria-current="page"':''} href="${esc(href(url))}" title="${esc(t)}" data-nav>${icon(ico)}<span class="nav-label">${esc(t)}</span></a>`).join('')}</nav><div class="sidebar-bottom"><div class="local-card"><div class="row"><span class="dot ${connected?'':'off'}"></span><strong>Local workspace</strong></div><p class="muted mt" style="margin-top:6px">코어 ${esc(env.version||'—')} · UI ${esc(bootstrap.uiVersion)}<br>데이터는 이 기기에 머뭅니다.</p></div></div>`;
 // 순서는 전체 프로젝트(조회) → 공통 기억 → 프로젝트 목록이며, 각 항목에 그 범위의 활성 기억 수를 함께 보여준다.
 const totals=bootstrap.factTotals||{};const factCount=n=>n===null||n===undefined?'':' · 기억 '+number(n)+'개';
 const scopeOption=(value,text)=>`<option value="${esc(value)}" ${value===selected?'selected':''}>${esc(text)}</option>`;
 const projectOptions=bootstrap.projects.map(p=>scopeOption('project:'+p.project,`${p.name} · ${p.project}${factCount(p.facts)}`)).join('');
 const known=new Set(['global','all',...bootstrap.projects.map(p=>'project:'+p.project)]);
 const scopeOptions=scopeOption('all','전체 프로젝트 (조회)'+factCount(totals.all))+scopeOption('global','공통 기억'+factCount(totals.global))+(projectOptions?`<optgroup label="프로젝트">${projectOptions}</optgroup>`:'')+(known.has(selected)?'':scopeOption(selected,title));
 document.querySelector('#topbar').innerHTML=`<div class="scope-wrap"><button class="icon-btn mobile-menu" data-action="mobile-menu" aria-label="주 메뉴 열기">${icon('menu')}</button><div class="scope-icon">${icon(all?'layers':global?'memory':'folder')}</div><div class="grow"><label><span class="hidden">조회 프로젝트</span><select id="scope-select" class="scope-select" aria-label="조회 범위">${scopeOptions}</select></label><div class="scope-line truncate" title="${esc(project)}">${all?'모든 프로젝트와 공통 기억 · 조회 전용':global?'프로젝트에 종속되지 않는 기억':esc(project)}${scope.workspace?' · 작업 공간 선택됨':''}${scope.workstream?' · 작업 흐름 선택됨':''}</div></div><span class="scope-global" id="scope-hint" title="${esc(SCOPE_HINT)}">${icon('info')}주입: 현재 프로젝트 + 공통 기억</span>${!global&&!all?`<label class="scope-global"><input type="checkbox" id="include-global" ${scope.includeGlobal!=='0'?'checked':''}> 공통 포함</label><button class="icon-btn" data-action="scope-advanced" aria-label="워크스페이스와 작업 흐름 범위">${icon('layers')}</button>`:''}</div><div class="topbar-right"><button class="global-search" data-action="global-search" aria-label="통합 검색">${icon('search')}<span>기억과 대화 검색</span><kbd>⌘ K</kbd></button><span class="connection" id="connection"><i class="dot ${connected?'':'off'}"></i>${connected?'UI 연결됨':'연결 확인 중'}</span><button class="icon-btn" data-action="refresh" title="${unread?'새 변경 있음 · 새로고침':'새로고침'}" aria-label="새로고침">${icon(unread?'clock':'refresh')}</button><button class="icon-btn" data-action="theme" aria-label="밝기 테마 전환">${icon(document.documentElement.dataset.theme==='dark'?'sun':'moon')}</button></div>`;
 document.querySelector('#scope-select').addEventListener('change',e=>changeScope(e.target.value));document.querySelector('#include-global')?.addEventListener('change',e=>{update({includeGlobal:e.target.checked?'1':'0',offset:null,panel:null,item:null,panelTab:null});persistScope();});
}
function persistScope(){try{localStorage.setItem('memex.workspace.scope',JSON.stringify(currentScope()));}catch{}}
function changeScope(value){const u=new URL(location.href);for(const k of [...scopeKeys,...panelKeys,'session','highlight','turnOffset','offset','focus','taxonomy','domain','file'])u.searchParams.delete(k);if(value.startsWith('project:')){u.searchParams.set('scope','project');u.searchParams.set('project',value.slice(8));u.searchParams.set('includeGlobal','1');}else u.searchParams.set('scope',value);go(u.href);persistScope();}
async function render(force=false){if(!bootstrap)return;renderShell();const key=pageKey();if(force||lastPageKey!==key){lastPageKey=key;const generation=++pageGeneration;pageAbort?.abort();pageAbort=new AbortController();pageCleanup?.();pageCleanup=null;loading=true;main.innerHTML=skeleton();const module=routes[routePath()];document.title=`${navigation.find(n=>n[0]===routePath())?.[2]||'Memex'} · Memex Workspace`;try{const result=module?await module.render(context(pageAbort.signal)):{html:empty('페이지를 찾을 수 없습니다','주소를 확인하세요.',linkBtn('개요로 이동','left',href('/')))};if(generation!==pageGeneration)return;main.innerHTML=(bootstrap.environment.demo?banner('<strong>검증용 예제 데이터</strong> · 실제 사용자 저장소가 아닙니다. 코어 변경·관리 실행은 이 미리보기에서 비활성화됩니다.','warning'):'')+result.html;pageCleanup=result.cleanup||null;result.mount?.(main);}catch(e){if(generation!==pageGeneration)return;main.innerHTML=errorCard(e)+`<div class="mt">${linkBtn('관리 · 진단','settings',href('/settings',{tab:'diagnostics'}))}</div>`;}finally{if(generation===pageGeneration)loading=false;}}await renderDrawer();}
async function renderDrawer(force=false){const p=new URL(location.href).searchParams,kind=p.get('panel'),id=p.get('item');const key=JSON.stringify([currentScope(),kind,id,p.get('panelTab')]);if(!force&&key===lastDrawerKey)return;lastDrawerKey=key;const generation=++drawerGeneration;drawerAbort?.abort();drawerCleanup?.();drawerCleanup=null;if(!kind||!id){if(detail.open)detail.close();return;}drawerAbort=new AbortController();if(!detail.open)detail.showModal();detail.setAttribute('aria-labelledby','detail-title');detail.innerHTML=`<header class="drawer-head"><div class="spread"><h2 id="detail-title">상세 불러오는 중</h2><button class="icon-btn" data-action="close-detail" aria-label="닫기">${icon('close')}</button></div></header><div class="drawer-body">${skeleton()}</div>`;try{const result=await renderDetail(context(drawerAbort.signal),kind,id,payloads.get(`${kind}:${id}`));if(generation!==drawerGeneration)return;detail.innerHTML=result.html;drawerCleanup=result.cleanup;result.mount?.(detail);detail.scrollTop=0;}catch(e){if(generation!==drawerGeneration)return;detail.innerHTML=`<header class="drawer-head"><div class="spread"><h2 id="detail-title">상세 조회</h2><button class="icon-btn" data-action="close-detail" aria-label="닫기">${icon('close')}</button></div></header><div class="drawer-body">${errorCard(e)}</div>`;}}
function showModal(title,body,submitText,onSubmit,submitClass='primary'){if(modal.open)modal.close();modal.innerHTML=`<form id="modal-form" class="modal-form"><header class="modal-head"><h2 id="modal-title">${esc(title)}</h2><button type="button" class="icon-btn" data-action="close-modal" aria-label="닫기">${icon('close')}</button></header><div class="modal-body">${body}</div><div class="modal-error" role="alert"></div><footer class="modal-footer">${btn('취소',null,'type="button" data-action="close-modal"')}${btn(submitText,null,'type="submit"',submitClass)}</footer></form>`;modal.setAttribute('aria-labelledby','modal-title');modal.showModal();const form=modal.querySelector('form');form.addEventListener('submit',async e=>{e.preventDefault();const submit=form.querySelector('[type="submit"]');submit.disabled=true;form.querySelector('.modal-error').textContent='';try{await onSubmit(new FormData(form));if(modal.contains(form))modal.close();}catch(e){form.querySelector('.modal-error').textContent=e.message;submit.disabled=false;}});}
async function advancedScope(){const ctx=context();try{const d=await ctx.api('scopes');showModal('상세 조회 범위',banner('프로젝트 수준의 기억은 계속 포함합니다. 선택한 워크스페이스·작업 흐름의 미승격 기억을 추가로 볼 수 있습니다.')+`<label class="field">워크스페이스<select name="workspace">${options([['','프로젝트 전체'],...d.workspaces.map(w=>[w.workspace_id,`${w.canonical_path} · ${w.branch||'branch 없음'}`])],ctx.scope.workspace)}</select></label><label class="field">작업 흐름<select name="workstream">${options([['','선택 안 함'],...d.workstreams.map(w=>[w.workstream_id,`${w.branch_hint||'작업'} · ${short(w.session_id)} · ${short(w.workstream_id)}`])],ctx.scope.workstream)}</select></label>`,'범위 적용',fd=>{update({workspace:fd.get('workspace'),workstream:fd.get('workstream'),offset:null,session:null,turnOffset:null,panel:null,item:null});persistScope();});}catch(e){toast(e.message);}}
let searchAbort,searchTimer;
function globalSearch(){if(modal.open)modal.close();modal.innerHTML=`<header class="modal-head"><h2 id="modal-title">현재 범위에서 검색</h2><button class="icon-btn" data-action="close-modal" aria-label="닫기">${icon('close')}</button></header><div class="modal-body" style="padding-bottom:15px"><label class="search-field" style="max-width:none">${icon('search')}<input id="command-input" type="search" placeholder="기억 또는 대화 내용" maxlength="500" aria-label="통합 검색어"></label><p class="caption mt" style="margin-top:10px">저장된 내용 검색 · 모델을 호출하지 않습니다.</p></div><div class="command-results" id="command-results">${empty('검색어를 입력하세요','기억과 대화를 함께 검색합니다.','','search')}</div>`;modal.setAttribute('aria-labelledby','modal-title');modal.showModal();const input=modal.querySelector('input'),root=modal.querySelector('#command-results');input.focus();input.addEventListener('input',()=>{clearTimeout(searchTimer);searchAbort?.abort();const q=input.value.trim();if(!q){root.innerHTML=empty('검색어를 입력하세요','현재 선택 범위만 조회합니다.','','search');return;}searchTimer=setTimeout(async()=>{searchAbort=new AbortController();const ctx=context(searchAbort.signal);root.innerHTML='<div class="card-body muted">검색 중…</div>';try{const [f,e]=await Promise.all([ctx.api('facts',{q,limit:6}),ctx.api('exchanges',{q,limit:6,searchMode:'contains'})]);if(input.value.trim()!==q)return;root.innerHTML=`<div class="command-group">기억 · ${number(f.total)}개</div>${f.items.map(x=>`<button class="command-item" data-search-fact="${esc(x.id)}">${icon('memory')}<div><p>${esc(prefs.korean&&x.fact_kr?x.fact_kr:x.fact)}</p><span class="meta">${esc(nameCategory(x.category))} · ${esc(short(x.id))}</span></div></button>`).join('')||'<div class="card-body muted">일치하는 기억 없음</div>'}<div class="command-group">대화 턴 · ${number(e.total)}개</div>${e.items.map(x=>`<button class="command-item" data-search-exchange="${esc(x.id)}">${icon('chat')}<div><p>${esc((x.user_message||x.assistant_message||'').slice(0,160))}</p><span class="meta">${esc(basename(x.project))} · ${esc(short(x.session_id))}</span></div></button>`).join('')||'<div class="card-body muted">일치하는 대화 없음</div>'}`;}catch(e){if(!searchAbort.signal.aborted)root.innerHTML=errorCard(e);}},250);});}
const nameCategory=k=>({decision:'결정',preference:'선호',constraint:'제약',pattern:'패턴',knowledge:'지식'})[k]||k;
document.addEventListener('click',e=>{const target=e.target.closest('a,button');if(!target)return;if(target.matches('a[data-nav]')){if(e.button||e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;e.preventDefault();if(modal.open)modal.close();go(target.href);return;}const d=target.dataset;try{if(d.searchFact||d.searchExchange){modal.close();open(d.searchFact?'fact':'exchange',d.searchFact||d.searchExchange);return;}if(d.fact){open('fact',d.fact);return;}if(d.exchange){open('exchange',d.exchange);return;}if(d.job){open('job',d.job);return;}if(d.operation){open('operation',d.operation);return;}if(d.event){const x=JSON.parse(d.event);open('event',x.id,x);return;}if(d.attempt){const x=JSON.parse(d.attempt);open('attempt',x.attempt_id,x);return;}if(d.command){commandModal(context(),d.command);return;}if(d.page!==undefined){update({[d.pageKey||'offset']:d.page});return;}if(d.paramKey){update({[d.paramKey]:d.paramValue,offset:null});return;}switch(d.action){case 'refresh':unread=false;lastDrawerKey='';render(true);break;case 'refresh-bootstrap':boot(true);break;case 'close-detail':closeDetail();break;case 'close-modal':modal.close();break;case 'theme':savePrefs({theme:document.documentElement.dataset.theme==='dark'?'light':'dark'});break;case 'mobile-menu':document.querySelector('#sidebar').classList.toggle('mobile-open');break;case 'global-search':globalSearch();break;case 'scope-advanced':advancedScope();break;case 'scope-all':changeScope('all');break;case 'toggle-language':savePrefs({korean:!prefs.korean});render(true);break;case 'toggle-live':savePrefs({live:!prefs.live});render(true);break;}}catch(error){toast(error.message);}});
document.addEventListener('submit',e=>{if(!e.target.matches('form[data-filter]'))return;e.preventDefault();const changes={offset:null};for(const [k,v]of new FormData(e.target))changes[k]=String(v).trim()||null;update(changes);});
detail.addEventListener('cancel',e=>{e.preventDefault();closeDetail();});modal.addEventListener('close',()=>{clearTimeout(searchTimer);searchAbort?.abort();});
document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();globalSearch();}});window.addEventListener('popstate',()=>render());
setInterval(()=>{if(prefs.live&&routePath()==='/activity'&&document.visibilityState==='visible'&&!detail.open&&!modal.open&&!loading&&!document.activeElement?.matches('input,select,textarea')){const y=window.scrollY;render(true).then(()=>window.scrollTo({top:y}));}},10000);
function connectEvents(){eventSource?.close();eventSource=new EventSource('/api/v2/events');eventSource.addEventListener('connected',()=>{connected=true;renderShell();});eventSource.addEventListener('change',()=>{unread=true;renderShell();});eventSource.onerror=()=>{connected=false;renderShell();};}
async function boot(refresh=false){try{bootstrap=await request('bootstrap');setToken(bootstrap.csrfToken);if(refresh){lastPageKey='';lastDrawerKey='';}connectEvents();await render(true);}catch(e){main.innerHTML=errorCard(e)+`<p class="caption mt">서버가 실행 중인지 확인하고 Memex 서버의 동일한 주소에서 다시 여세요.</p>`;main.querySelector('[data-action="refresh"]')?.addEventListener('click',()=>boot(true));}}
canonicalize();boot();
